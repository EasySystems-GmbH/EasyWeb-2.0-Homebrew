# EasyWeb 2.0 Homebrew

Homebrew formula repository for the EasyWeb CLI.

Install:

```bash
brew install --formula "https://raw.githubusercontent.com/EasySystems-GmbH/EasyWeb-2.0-Homebrew/main/Formula/easyweb.rb"
```
